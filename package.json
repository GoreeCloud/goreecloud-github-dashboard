{
  "name": "goreecloud-github-dashboard",
  "version": "0.2.0-dev",
  "private": true,
  "description": "Private GoreeCloud dashboard for GitHub repository activity and health.",
  "license": "MIT",
  "type": "module",
  "scripts": {
    "test": "node --test",
    "check": "node scripts/validate.mjs && node --check public/app.js && node --check functions/_middleware.js && node --check functions/api/dashboard.js && node --check functions/lib/github.js"
  },
  "engines": {
    "node": ">=20"
  }
}
